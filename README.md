### rand.om

rand.om is a minimilst site that enables users to fetch random blocks from their choice of are.na channels. all the user has to do is paste in their link and get to refreshing to see random blocks!
