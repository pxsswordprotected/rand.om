### rand.om

rand.om is a minimalist site that enables users to fetch random blocks from their choice of are.na channels. all the user has to do is paste in a link to their desired are.na channel and get to refreshing to see random blocks!
